[Core]
versions = ["1", "lts"]

# Core 32-bit dropped: Tracker→NNlib→BFloat16s LLVM soft-promote fails on i686
# (JuliaMath/BFloat16s#127). Re-enable after that ships.

[LineSearchesJL]
versions = ["1", "lts"]

[QA]
versions = ["lts", "1"]
in_all = false
