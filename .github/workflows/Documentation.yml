name: Documentation

on:
  push:
    branches:
      - main
      - 'release-'
    tags: ["*"]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: "SciML/.github/.github/workflows/documentation.yml@v1"
    with:
      debug-documenter: true
    secrets: "inherit"
